<div class="Upload Form">
  <h3>Submit your order:</h3>
  <%= simple_form_for [@drink, @order] do |form| %>
    <%= form.input :quantity, label: "Quantity" %>
    <%= form.button :submit %>
  <% end %>
</div>

<%# need some help on getting submit to link to order confirmation %>
<%# link_to "See order", order_path(order) %>
