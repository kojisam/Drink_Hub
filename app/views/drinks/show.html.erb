<div>

    <p><%= @drink.name %></p>

    <%= cl_image_tag @drink.photo.key, height: 300, width: 400, crop: :fill %>


    <p><%= @drink.description %></p>
    <p><%= @drink.price %></p>

      <%# <%= link_to "Drinks Home",
        drink_path(@drink)
      %>

    <button type="button" class="btn btn-danger m-1"><%= link_to "See our selection of drinks", "/drinks" %>
    <button type="button" class="btn btn-danger"><%= link_to "Buy Now", new_drink_order_path(@drink)%>
    <%# <%= link_to "Buy now", new_drink_order_path(@drink) %>
</div>
